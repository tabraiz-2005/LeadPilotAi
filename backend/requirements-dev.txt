# Lightweight local dev install for the Backend Lead role.
# Versions are NOT tightly pinned here (unlike requirements.txt, which Docker uses)
# so pip can pick whatever release has a prebuilt wheel for your local Python version.

fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.9
python-dotenv>=1.0
httpx>=0.27
pandas>=2.2
python-multipart>=0.0.9
pytest>=8.3
pytest-mock>=3.14
